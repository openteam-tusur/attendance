<%= content_for :page_title do %>
  <h1 class="text-center">Группы факультета <%= @faculty %></h1>
<% end %>

<ul>
  <% @groups.each do |course, groups| %>
    <li>
    <h3><%= course %> курс</h3>
      <ul>
        <% groups.each do |group| %>
          <li><%= link_to group, dean_group_path(group.number) %></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>
