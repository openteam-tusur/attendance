<%= render :partial => 'search' %>
<% @disruptions.each do |group| %>
  <% next unless group.results.any? %>
  <%= render :partial => 'disruption', :locals => { :group => group } %>
<% end %>
<%= paginate @disruptions %>
