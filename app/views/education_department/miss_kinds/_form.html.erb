<%= simple_form_for [:education_department, @miss_kind],
                    :html => { :class => 'form-horizontal form-permission' },
                    :wrapper => :horizontal_form do |f| %>

  <%= f.input :kind %>

  <%= f.submit 'Сохранить', :class => 'btn btn-primary btn-sm' %>
  <%= link_to 'Отмена', education_department_miss_kinds_path, :class => 'btn btn-default btn-sm' %>
<% end %>
